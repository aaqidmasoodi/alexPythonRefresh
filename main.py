# __init__.py tells python that the directory is a python package
# packages can have multiple module (python files) inside it.
# from mypackage import module1
from mypackage.module1 import get_database


# module1.get_database()
